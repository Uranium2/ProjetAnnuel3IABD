import numpy as np
import tensorflow.keras as tf


if __name__ == "__main__":
    print("linear simple model")

    # DATASET
    x = np.array([
        [1, 1],
        [2, 3],
        [3, 3]
    ])
    y = np.array([
        1,
        -1,
        -1
    ])

    # MODEL
    model = tf.Sequential()

    # LAYERS


    # COMPILE


    # FIT
